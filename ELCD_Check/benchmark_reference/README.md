# Benchmark reference catalog

`ELCD_Process_Catalog_Benchmark.xlsx` is the archived process catalog associated with the completed four-model selection experiment. It is retained for audit/comparison only.

The frozen manuscript production catalog is `../ELCD_Process_Catalog.xlsx` and is independently protected by `../ELCD_Catalog_Lock.json`.
